// Pagination configs
export const pageSize = 5;
// Authentication configs
export const emailErrorMessage = 'Email should be valid email e.g. user@example.com';
export const passwordErrorMessage = 'Password should be at least 8 characters with 1 Upper Case, 1 Lower Case and at least 1 number';
export const confirmPasswordErrorMessage = "Passwords didn't match";